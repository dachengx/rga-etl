apiVersion: 1

providers:
  - name: 'RGA dashboards'
    orgId: 1
    folder: 'RGA'
    type: file
    options:
      # This path is inside the container
      path: '/etc/grafana/provisioning/dashboards'
    updateIntervalSeconds: 10
    allowUiUpdates: true
